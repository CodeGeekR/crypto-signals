---

---

<div
  class="hero-cta flex flex-col items-center justify-center w-full max-w-md mx-auto animate-slide-up"
  style="animation-delay: 0.8s;"
>
  <!-- Primary CTA Button - Lista de Espera -->
  <a
    href="#contact"
    id="cta-waitlist"
    class="group relative overflow-hidden w-full px-8 py-5 bg-gradient-to-r from-red-500 to-orange-500 text-white text-lg font-bold rounded-xl shadow-xl hover:shadow-2xl transition-all duration-300 hover:scale-105 hover:from-red-400 hover:to-orange-400 inline-block text-center"
    aria-label="Ir a la lista de espera gratuita"
  >
    <!-- Background glow effect -->
    <div
      class="absolute inset-0 bg-gradient-to-r from-red-400/20 to-orange-400/20 rounded-xl blur-lg scale-110 opacity-0 group-hover:opacity-100 transition-opacity duration-300"
    >
    </div>

    <!-- Button content -->
    <span class="relative z-10 flex items-center justify-center space-x-2">
      <span class="hidden sm:inline">🔥 Reservar Mi Spot Gratuito</span>
      <span class="sm:hidden">🔥 Spot Gratuito</span>
      <svg
        class="w-5 h-5 transform group-hover:translate-x-1 transition-transform"
        fill="none"
        stroke="currentColor"
        viewBox="0 0 24 24"
        aria-hidden="true"
      >
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
      </svg>
    </span>

    <!-- Pulse effect -->
    <div
      class="absolute inset-0 rounded-xl bg-gradient-to-r from-red-400 to-orange-400 opacity-0 group-hover:opacity-20 animate-pulse"
    >
    </div>
  </a>

  <!-- Urgency Text -->
  <div class="mt-4 text-center">
    <p class="text-sm text-gray-300 flex items-center justify-center space-x-2">
      <span class="w-2 h-2 bg-accent-green rounded-full animate-pulse"></span>
      <span>
        <strong class="text-white">Sin tarjeta de crédito</strong> •
        <span class="text-accent-green">por tiempo limitado</span> • <br />
      </span>
    </p>
  </div>

  <!-- Social Proof Mini -->
  <div class="mt-3 text-center">
    <p class="text-xs text-gray-400">
      <span class="text-accent-gold font-medium">+2,847 traders</span> se registraron esta semana
    </p>
  </div>
</div>

<script>
  // TypeScript declarations
  declare global {
    interface Window {
      gtag?: (...args: any[]) => void;
    }
  }

  document.addEventListener("DOMContentLoaded", () => {
    const waitlistBtn = document.getElementById("cta-waitlist");

    waitlistBtn?.addEventListener("click", (e) => {
      e.preventDefault();

      // Scroll suave hacia la sección de contacto
      const contactSection = document.getElementById("contact");
      if (contactSection) {
        const headerElement = document.querySelector(".header-main") as HTMLElement;
        const headerHeight = headerElement?.offsetHeight || 80;
        const targetPosition = contactSection.offsetTop - headerHeight;

        window.scrollTo({
          top: targetPosition,
          behavior: "smooth",
        });

        // Track CTA click para analytics
        if (typeof window.gtag !== "undefined") {
          window.gtag("event", "cta_click", {
            event_category: "engagement",
            event_label: "waitlist_hero_cta",
            value: 1,
          });
        }

        console.log("Redirecting to waitlist section");
      }
    });
  });
</script>
