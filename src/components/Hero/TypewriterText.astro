---
const words = [
  "Señales IA Institucionales",
  "Trading Algorítmico", 
  "Gestión de Riesgo Pro"
];

const colors = [
  "text-accent-green",
  "text-accent-gold",
  "text-accent-blue"
];
---

<div class="flex flex-col items-center">
  <span class="text-2xl text-gray-300 mb-4">Domina el mercado con</span>
  <div class="scrolling-words-container">
    <div class="scrolling-words-box">
      <ul>
        {words.map((word, index) => (
          <li class={colors[index]}>{word}</li>
        ))}
        <li class={colors[0]}>{words[0]}</li>
      </ul>
    </div>
  </div>
</div>

<style>
  .scrolling-words-container {
    display: flex;
    align-items: center;
    font-weight: 600;
  }

  .scrolling-words-box {
    height: 58px;
    overflow: hidden;
  }

  .scrolling-words-box ul {
    margin: 0;
    padding: 0;
    animation: scrollUp 9s infinite;
  }

  .scrolling-words-box ul li {
    height: 58px;
    list-style: none;
    font-size: 2.5rem;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  @keyframes scrollUp {
    15%, 25% {
      transform: translateY(-25%);
    }
    40%, 50% {
      transform: translateY(-50%);
    }
    65%, 75% {
      transform: translateY(-75%);
    }
    90%, 100% {
      transform: translateY(-100%);
    }
  }
</style>