---
import ParticleBackground from "./ParticleBackground.astro";
import TypewriterText from "./TypewriterText.astro";
import CTAButtons from "./CTAButtons.astro";
import CountdownTimer from "./CountdownTimer.astro";
---

<section id="hero" class="relative min-h-screen flex items-center justify-center overflow-hidden bg-gradient-hero">
  <ParticleBackground />

  <!-- Gradient Overlays -->
  <div class="absolute inset-0 bg-gradient-to-b from-transparent via-primary-900/20 to-primary-900/60"></div>
  <div class="absolute inset-0 bg-gradient-to-r from-primary-900/30 via-transparent to-primary-900/30"></div>

  <div class="relative w-full">
    <div class="container mx-auto px-4 sm:px-6 lg:px-8 py-16 lg:py-24">
      <div class="max-w-6xl mx-auto text-center">
        <!-- Viral Marketing Badge - Lista de Espera -->
        <div class="hero-badge-container mt-4 lg:mt-6 mb-16 lg:mb-20 animate-fade-in">
          <div
            class="hero-badge inline-flex items-center bg-gradient-to-r from-red-500/20 to-orange-500/20 backdrop-blur-md border border-red-400/30 rounded-full px-4 py-3 sm:px-8 sm:py-4 shadow-lg hover:shadow-xl transition-all duration-300 hover:scale-105"
          >
            <div class="flex items-center space-x-2 sm:space-x-3">
              <!-- Pulsing dot -->
              <div class="relative">
                <span class="w-3 h-3 bg-red-400 rounded-full block animate-pulse"></span>
                <span class="absolute top-0 left-0 w-3 h-3 bg-red-400 rounded-full animate-ping"></span>
              </div>

              <!-- Urgency content -->
              <div class="flex flex-col sm:flex-row sm:items-center sm:space-x-2 text-left sm:text-center">
                <span class="text-xs sm:text-sm font-bold text-red-200 uppercase tracking-wide">
                  <span class="hidden sm:inline">🔥 ACCESO GRATUITO LIMITADO</span>
                  <span class="sm:hidden">🔥 ACCESO LIMITADO</span>
                </span>
                <span class="hidden sm:inline text-white/80 text-sm">•</span>
                <span class="text-xs sm:text-sm font-medium text-white/90">
                  <span class="hidden lg:inline">Solo 247 spots restantes</span>
                  <span class="hidden sm:inline lg:hidden">247 spots restantes</span>
                  <span class="sm:hidden">247 restantes</span>
                </span>
              </div>
            </div>
          </div>

          <!-- Countdown Timer con diseño de fichas dominó -->
          <div class="mt-3 sm:mt-4">
            <CountdownTimer targetDate="30" className="countdown-domino-timer" />
          </div>
        </div>

        <!-- Main Content -->
        <div class="space-y-8 lg:space-y-12">
          <!-- Hero Title -->
          <h1 class="hero-title animate-slide-up-delayed">
            <span
              class="block font-hero text-3xl sm:text-4xl md:text-6xl lg:text-7xl xl:text-8xl font-bold leading-[1.1] tracking-tight"
            >
              El Trading de <span class="text-gradient">Wall Street,</span>
            </span>
            <span
              class="block font-hero text-3xl sm:text-4xl md:text-6xl lg:text-7xl xl:text-8xl font-bold leading-[1.1] tracking-tight mt-2 lg:mt-4"
            >
              Ahora en tu <span class="text-accent-gold">Bolsillo</span>
            </span>
          </h1>

          <!-- Typewriter Section -->
          <div class="hero-subtitle animate-slide-up-delayed-2 mt-8 lg:mt-12">
            <TypewriterText />
          </div>

          <!-- Description -->
          <p
            class="hero-description text-base sm:text-lg md:text-xl lg:text-2xl text-gray-300 max-w-4xl mx-auto leading-relaxed animate-slide-up-delayed-3 px-4 sm:px-0 mt-8 lg:mt-10"
          >
            Señales de trading institucionales impulsadas por <span class="text-accent-green font-semibold"
              >Inteligencia Artificial</span
            > para maximizar tus ganancias en el mercado cripto más volátil de la historia.
          </p>
        </div>

        <!-- Performance Metrics -->
        <div
          class="hero-metrics grid grid-cols-3 gap-4 md:gap-8 lg:gap-12 max-w-2xl mx-auto mt-12 lg:mt-16 animate-slide-up-delayed-4"
        >
          <div class="metric-item text-center">
            <div class="text-2xl sm:text-3xl lg:text-4xl font-bold text-accent-green mb-1 lg:mb-2">89.7%</div>
            <div class="text-xs sm:text-sm text-gray-400 uppercase tracking-wide">Precisión IA</div>
          </div>
          <div class="metric-item text-center">
            <div class="text-2xl sm:text-3xl lg:text-4xl font-bold text-accent-gold mb-1 lg:mb-2">24/7</div>
            <div class="text-xs sm:text-sm text-gray-400 uppercase tracking-wide">Análisis Live</div>
          </div>
          <div class="metric-item text-center">
            <div class="text-2xl sm:text-3xl lg:text-4xl font-bold text-accent-blue mb-1 lg:mb-2">+347%</div>
            <div class="text-xs sm:text-sm text-gray-400 uppercase tracking-wide">ROI Promedio</div>
          </div>
        </div>

        <!-- Call to Action -->
        <div class="mt-12 lg:mt-20">
          <CTAButtons />
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  document.addEventListener("DOMContentLoaded", () => {
    // Cargar animaciones GSAP como fallback
    import("../../scripts/animations.ts")
      .then(({ initHeroAnimations }) => {
        initHeroAnimations();
      })
      .catch((error) => {
        console.log("GSAP animations not loaded, using CSS fallback", error);
        // Las animaciones CSS ya están funcionando como fallback
      });
  });
</script>

<style>
  /* Badge hover effects */
  .hero-badge:hover {
    @apply shadow-red-500/25;
  }

  /* Responsive spacing for badge container */
  .hero-badge-container {
    @apply transition-all duration-500 ease-out;
  }

  /* CSS Fallback Animations - Se ejecutan inmediatamente */
  .animate-fade-in {
    animation: fade-in 1s ease-out forwards;
  }

  .animate-slide-up-delayed {
    animation: slide-up 1s ease-out 0.3s forwards;
    opacity: 0;
  }

  .animate-slide-up-delayed-2 {
    animation: slide-up 1s ease-out 0.6s forwards;
    opacity: 0;
  }

  .animate-slide-up-delayed-3 {
    animation: slide-up 1s ease-out 0.9s forwards;
    opacity: 0;
  }

  .animate-slide-up-delayed-4 {
    animation: slide-up 1s ease-out 1.2s forwards;
    opacity: 0;
  }

  .animate-slide-up-delayed-5 {
    animation: slide-up 1s ease-out 1.5s forwards;
    opacity: 0;
  }

  @keyframes fade-in {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes slide-up {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Enhanced mobile responsiveness */
  @media (max-width: 480px) {
    .hero-badge {
      @apply px-3 py-2 max-w-[95vw] mx-auto;
    }

    .hero-badge-container {
      @apply mt-2 mb-12;
    }
  }

  @media (min-width: 481px) and (max-width: 768px) {
    .hero-badge-container {
      @apply mt-4 mb-16;
    }
  }

  @media (min-width: 769px) {
    .hero-badge-container {
      @apply mt-6 mb-20;
    }
  }
</style>
